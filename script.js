const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');
addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>24));
menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});
document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));
document.querySelectorAll('.filters button').forEach(button=>button.addEventListener('click',()=>{document.querySelector('.filters .active').classList.remove('active');button.classList.add('active');const filter=button.dataset.filter;document.querySelectorAll('.project').forEach(p=>p.classList.toggle('hide',filter!=='all'&&!p.classList.contains(filter)))}));
addEventListener('pointermove',e=>{const g=document.querySelector('.cursor-glow');g.style.left=e.clientX+'px';g.style.top=e.clientY+'px'});
const contactForm=document.querySelector('#contact-form');
contactForm.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(contactForm);const text=`Salaan ZAADAQ, waxaan rabaa inaan kaa codsado mashruuc.\n\nMagaca: ${data.get('name')}\nEmail: ${data.get('email')}\nNooca mashruuca: ${data.get('projectType')}\n\nFaahfaahin:\n${data.get('message')}`;window.open(`https://wa.me/252615156485?text=${encodeURIComponent(text)}`,'_blank','noopener');});
const loginModal=document.querySelector('#login-modal'),loginTrigger=document.querySelector('#login-trigger'),logoutTrigger=document.querySelector('#logout-trigger'),closeLogin=document.querySelector('#close-login'),loginForm=document.querySelector('#login-form'),switchAuth=document.querySelector('#switch-auth'),loginError=document.querySelector('#login-error'),registerOnly=document.querySelector('.register-only'),loginTitle=document.querySelector('#login-title'),loginDescription=document.querySelector('#login-description');
let isRegistering=false;
const updateLoginState=async()=>{try{const response=await fetch('/api/auth/me',{credentials:'same-origin'});if(!response.ok)throw new Error();const {user}=await response.json();loginTrigger.hidden=true;logoutTrigger.hidden=false;logoutTrigger.innerHTML=`Logout, ${user.name.split(' ')[0]} <span>↗</span>`;}catch{loginTrigger.hidden=false;logoutTrigger.hidden=true;}};
const setAuthMode=registering=>{isRegistering=registering;registerOnly.hidden=!registering;registerOnly.querySelector('input').required=registering;loginTitle.innerHTML=registering?'Create your<br>account.':'Welcome<br>back.';loginDescription.textContent=registering?'Create a secure ZAADAQ account.':'Log in securely to your ZAADAQ account.';loginForm.querySelector('button[type="submit"]').innerHTML=registering?'Create account <b>↗</b>':'Login <b>↗</b>';switchAuth.textContent=registering?'Already have an account? Log in':'New here? Create an account';loginError.textContent='';};
const openLogin=()=>{setAuthMode(false);loginModal.classList.add('open');loginModal.setAttribute('aria-hidden','false');loginForm.querySelector('input').focus();};
const closeLoginModal=()=>{loginModal.classList.remove('open');loginModal.setAttribute('aria-hidden','true');};
loginTrigger.addEventListener('click',openLogin);closeLogin.addEventListener('click',closeLoginModal);loginModal.addEventListener('click',event=>{if(event.target===loginModal)closeLoginModal();});
switchAuth.addEventListener('click',()=>setAuthMode(!isRegistering));
loginForm.addEventListener('submit',async event=>{event.preventDefault();loginError.textContent='';const data=new FormData(loginForm);const payload={email:data.get('loginEmail'),password:data.get('loginPassword'),name:data.get('loginName')};try{const response=await fetch(isRegistering?'/api/auth/register':'/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',body:JSON.stringify(payload)});const result=await response.json();if(!response.ok)throw new Error(result.error||'Unable to continue.');loginForm.reset();closeLoginModal();await updateLoginState();}catch(error){loginError.textContent=error.message==='Failed to fetch'?'Run this site through the ZAADAQ server before logging in.':error.message;}});
logoutTrigger.addEventListener('click',async()=>{await fetch('/api/auth/logout',{method:'POST',credentials:'same-origin'});await updateLoginState();});
addEventListener('keydown',event=>{if(event.key==='Escape')closeLoginModal();});updateLoginState();
